---
stages:
- prepare
- test
- lint
- build

yarn:install:
  artifacts:
    paths:
    - node_modules/
  cache:
    paths:
    - node_modules/
  image: docker.input-output.dk/docker/nodejs:7.5.0
  script:
  - yarn install
  stage: prepare
  tags:
  - kubernetes

lint:
  dependencies:
  - yarn:install
  image: docker.input-output.dk/docker/nodejs:7.5.0
  script:
  - yarn lint
  stage: lint
  tags:
  - kubernetes

test:7.5.0:
  dependencies:
  - yarn:install
  image: docker.input-output.dk/docker/nodejs:7.5.0
  script:
  - yarn test
  stage: test
  tags:
  - kubernetes

test:6.9.5:
  dependencies:
  - yarn:install
  image: docker.input-output.dk/docker/nodejs:6.9.5
  script:
  - yarn test
  stage: test
  tags:
  - kubernetes

build:
  artifacts:
    paths:
    - dist/
    - lib/
    - lib2015/
  dependencies:
    - yarn:install
  image: docker.input-output.dk/docker/nodejs:7.5.0
  script:
  - yarn build
  stage: build
  tags:
  - kubernetes
